# Personal Web

Static personal homepage (HTML / CSS / JS).

**Live site (GitHub Pages):** https://2501229548-lab.github.io/Personal-Web/

Enable **Settings → Pages → Branch `main` / folder `/ (root)`** if the site is not published yet.

## Push from your Mac (this environment cannot log in to GitHub)

In Terminal:

```bash
cd "/Users/elyjay/Desktop/个人主页"
git push -u origin main --force
```

Sign in when prompted (browser or token). After the push finishes, wait **1–3 minutes**, then open the live URL above.
